export interface DashboardSummary {
  totalMembers: number;
  activeMembers: number;
  dueToday: number;
  dueSoon: number;
  overdue: number;
  collectedThisMonth: number;
  pendingAmount: number;
}
